import React, { Fragment } from 'react';

function Login() { return (
    <Fragment>
        <div id={ 'header' }>Login</div>
        <div id={ 'content' }>
            login stuff pogu
        </div>
    </Fragment>
)}

export default Login;